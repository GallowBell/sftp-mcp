{
  "name": "sftp-mcp",
  "version": "0.1.0",
  "description": "MCP server to upload files over SFTP/FTPS with verified host keys and TLS",
  "type": "module",
  "bin": {
    "sftp-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "test:integration": "sh test/docker/gen-fixtures.sh && docker compose -f docker-compose.test.yml up -d --build --wait && sleep 3 && INTEGRATION=1 node --import tsx --test test/integration-*.test.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "basic-ftp": "^6.2.1",
    "ssh2-sftp-client": "^12.1.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/ssh2-sftp-client": "^9.0.6",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
